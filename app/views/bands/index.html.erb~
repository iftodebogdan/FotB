<% if user_signed_in? %>
<table class="table table-striped">  
<thead>  
  <tr>  
    <th>Band name</th>  
    <th># of Albums</th>  
    <th># of Topics</th>  
    <th># of Posts</th> 
    <th></th> 
  </tr>  
</thead>  
<tbody>
<% @bands.each do |band| %>
  <tr>
    <td><%= link_to band.band_name, band %></td>
    <td><%= band.albums.count %></td>
    <td><%= band.topics.count %></td>
    <td><%= band.posts.count %></td>
    <td><%= link_to 'Edit', edit_band_path(band), :class => "btn btn-mini" %>&nbsp;<%= link_to 'Delete', band, :class => "btn btn-mini", :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</tbody>  
</table>
<%= link_to "New band", '/bands/new', :class => "btn btn-mini" %>

<% else %>
<div class="center hero-unit">
  <h1>Welcome to FotB!</h1>

  <h2>
    Register or sign in below.
  </h2>

  <%= link_to "Register", '/users/sign_up', :class => "btn btn-large btn-primary" %>
  <%= link_to "Sign in", '/users/sign_in', :class => "btn btn-large btn-primary" %>
</div>
<% end %>

<% if false %>
<br/><br/><br/><br/><br/>

<table>
  <tr>
    <th>Band name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bands.each do |band| %>
  <tr>
    <td><%= band.band_name %></td>
    <td><%= link_to 'Show', band %></td>
    <td><%= link_to 'Edit', edit_band_path(band) %></td>
    <td><%= link_to 'Destroy', band, :method => :delete, :data => { :confirm => 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Band', new_band_path %>
<% end %>
