<p id="notice"><%= notice %></p>

<table class="table table-striped">  
<thead>  
  <tr>  
    <th>Username</th>
    <th>Post Content</th>  
  </tr>  
</thead>  
<tbody>
<% @topic.posts.each do |post| %>
  <tr>
    <td><%= link_to post.user.email, "/posts/#{post.id}" %></td>
    <td><%= post.post_content %></td>
  </tr>
<% end %>
</tbody>  
</table>
<%= link_to "New post", {:controller => "posts", :action => "new", :band_id => @topic.band.id, :album_id => @topic.album.id, :topic_id => @topic.id }, :class => "btn btn-mini" %>
<%= link_to "Back", "/albums/#{@topic.album.id}", :class => "btn btn-mini" %>

<br/><br/><br/><br/><br/>

<p>
  <b>Topic name:</b>
  <%= @topic.topic_name %>
</p>

<p>
  <b>Band:</b>
  <%= @topic.band_id %>
</p>

<p>
  <b>Album:</b>
  <%= @topic.album_id %>
</p>


<%= link_to 'Edit', edit_topic_path(@topic) %> |
<%= link_to 'Back', topics_path %>
